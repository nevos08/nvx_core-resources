Config = {}

Config.EnableStatusHUD = true  -- Show the UI for statuses. Needs nvx_status to work!
Config.EnableVehicleHUD = true -- Show the vehicle HUD

--! CSS values!
Config.Position = {
    top = "30px",
    right = "20px"
}
